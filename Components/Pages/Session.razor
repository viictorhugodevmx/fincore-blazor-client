@page "/session"
@rendermode InteractiveServer
@using FincoreBlazorClient.Models
@using FincoreBlazorClient.Services
@inject AuthSessionService AuthSessionService
@inject NavigationManager NavigationManager

<PageTitle>Sesión</PageTitle>

<section class="page-heading">
    <span class="eyebrow dark">Seguridad</span>
    <h1>Sesión simulada</h1>
    <p>Estado visual de autenticación usando un service scoped en Blazor.</p>
</section>

@if (!SessionState.IsAuthenticated)
{
    <section class="session-card">
        <span class="session-badge danger">No autenticado</span>
        <h2>No hay sesión activa</h2>
        <p>Inicia sesión para simular acceso al portal bancario.</p>

        <button type="button" class="primary-button" @onclick="GoToLogin">
            Ir al login
        </button>
    </section>
}
else
{
    <section class="session-card">
        <span class="session-badge success">Autenticado</span>
        <h2>@SessionState.Email</h2>
        <p>Sesión iniciada el @SessionState.LoggedAtUtc?.ToString("dd MMM yyyy HH:mm") UTC</p>

        <button type="button" class="secondary-danger-button" @onclick="HandleLogout">
            Cerrar sesión
        </button>
    </section>
}

@code {
    private AuthSessionState SessionState { get; set; } = new();

    protected override void OnInitialized()
    {
        SessionState = AuthSessionService.GetSession();
    }

    private void GoToLogin()
    {
        NavigationManager.NavigateTo("/login");
    }

    private void HandleLogout()
    {
        AuthSessionService.Logout();
        SessionState = AuthSessionService.GetSession();
    }
}
